# No external dependencies — this backend runs on Python 3.10+ standard
# library alone (http.server, sqlite3, hashlib/hmac, xml.etree). Nothing to
# install, nothing to patch for supply-chain vulnerabilities.
#
# Optional, for later:
#   requests          # nicer HTTP client than urllib for fetch_990.py
#   gunicorn + flask  # if you outgrow the stdlib server under real load
#   psycopg2-binary   # if you move from SQLite to Postgres for multi-instance deploys
