# Moved from SQLite to Postgres (Neon) so data survives Render free-tier
# deploys/restarts, which wipe any on-disk SQLite file. Everything else is
# still stdlib (http.server, hashlib/hmac, xml.etree) - nothing else to
# patch for supply-chain vulnerabilities.
psycopg2-binary
